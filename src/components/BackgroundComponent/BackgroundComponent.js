import { StyledBackgroundComponent } from "./BackgroundComponentStyles";

export const BackgroundComponent = ({ children }) => {
  return <StyledBackgroundComponent>{children}</StyledBackgroundComponent>;
};
